<div class=playsits>

<% session[:sets].each do |setname, values| %>
	<div class=playsits>
	<ul class=playlists>
	<li> <h1> Playlist: <%= setname%> </h1> <%= session[:sets][setname.to_sym]["description"]%> 
	<form action=<%= "/sets/" + setname.to_s %> method="get">
		<input type="submit" value="View"> </form> </li> 
	</form>
	</ul></div>
<% end %>
	
</ul>